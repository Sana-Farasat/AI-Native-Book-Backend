# Core FastAPI & Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.6

# Async & HTTP
httpx>=0.27.0
asyncio

# OpenAI & Embeddings
openai>=1.47.0

# Tiktoken - Latest (Python 3.13 compatible, pre-built wheels)
tiktoken>=0.8.0

# Qdrant Client (Async)
qdrant-client>=1.11.0

# Postgres - Psycopg3 (Neon recommended, no build errors)
psycopg[binary]>=3.2.0
# OR simply: psycopg>=3.2.0 if you want source build (but binary better)

# LangChain for RAG pipeline
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0

# Markdown/MDX parsing
#frontmatter>=0.5.0
markdown>=3.6

# Markdown/MDX parsing - Correct & Active Package
python-frontmatter>=1.1.0

# Utilities
python-dotenv>=1.0.1
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Optional but helpful
tqdm>=4.66.5

# Sentence transformer
sentence-transformers==2.7.0
torch==2.2.2